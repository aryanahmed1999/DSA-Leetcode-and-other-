class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int size = nums.size();
        int element = 0;
        for(int i = 0 ; i < size ; i++){
            element = element^nums[i];
            }
            return element;
        }
    };